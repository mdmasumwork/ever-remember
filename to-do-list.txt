Problem
====================

1. When the user clicks to generate the first version, and they are blocked by payment overlay, 
2. After payment, in additional question fields if the user click skip button, it should ignore the field vlaues.